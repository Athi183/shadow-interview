# Shared asset scope

Store shared visual assets here as the broader product evolves—for example,
logos, screenshots, and social preview images. The Milestone 1 extension keeps
its dedicated icon in `extension/assets/icons/` and uses no external assets.
